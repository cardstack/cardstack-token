// const CardStackToken = artifacts.require("./CardStackToken.sol");

contract('CstLedger', function(/*accounts*/) {

  describe("ledger", function() {
    xit("allows owner to add an admin", async function() {
    });

    xit("allows owner to remove an admin", async function() {
    });

    xit("non-owner cannot add admins", async function() {
    });

    xit("allows admin to mint tokens", async function() {
    });

    xit("allows admin to transfer tokens", async function() {
    });

    xit("does not allow transfer of more tokens than in the sender's account", async function() {
    });

    xit("allows admin to debit accounts", async function() {
    });

    xit("allows admin to credit accounts", async function() {
    });

    xit("does not allow crediting more tokens than are in the account", async function() {
    });

    xit("non-admins cannot credit accounts", async function() {
    });

    xit("non-admins cannot debit accounts", async function() {
    });

    xit("non-admins cannot mint tokens", async function() {
    });

    xit("non-admins cannot transfer tokens", async function() {
    });
  });
});
