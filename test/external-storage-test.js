// const CardStackToken = artifacts.require("./CardStackToken.sol");

contract('ExternalStorage', function(/*accounts*/) {

  describe("external storage", function() {
    xit("allows owner to add an admin", async function() {
    });

    xit("allows owner to remove an admin", async function() {
    });

    xit("non-owner cannot add admins", async function() {
    });

    xit("allows admin to set uint value", async function () {
    });

    xit("does not allow non-admin to set uint value", async function() {
    });

    xit("gets uint value", async function () {
    });

    xit("allows admin to set bytes32 value", async function () {
    });

    xit("does not allow non-admin to set bytes32 value", async function() {
    });

    xit("gets bytes32 value", async function () {
    });

    xit("allows admin to set address value", async function () {
    });

    xit("does not allow non-admin to set address value", async function() {
    });

    xit("gets address value", async function () {
    });

    xit("allows admin to set bytes value", async function () {
    });

    xit("does not allow non-admin to set bytes value", async function() {
    });

    xit("gets bytes value", async function () {
    });

    xit("allows admin to set boolean value", async function () {
    });

    xit("does not allow non-admin to set boolean value", async function() {
    });

    xit("gets boolean value", async function () {
    });

    xit("allows admin to set int value", async function () {
    });

    xit("does not allow non-admin to set int value", async function() {
    });

    xit("gets int value", async function () {
    });
  });
});
